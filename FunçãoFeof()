#include <stdio.h>
int main()
{   
    FILE *fp;
    fp=fopen("arquivo.txt","r");
    if("fp"){
        printf("Erro na abertura do arquivo");
        exit(1);
    }
    while (!feof(fp)){
        printf("%c",(char)getc(fp));
        fclose(fp);
    }
        
   return 0; 
}
