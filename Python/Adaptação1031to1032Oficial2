/*Agora a função is_prime utiliza um contador cont que é incrementado a cada iteração do for caso o 
número seja divisível pelo índice atual. Se o contador ultrapassar o valor 2, significa que o número 
tem mais de dois divisores e não é primo, então a função retorna False. Caso contrário, o número é 
primo e a função retorna True.*/


def is_prime(num):
    if num <= 1:
        return False
    cont = 0
    for i in range(1, num+1):
        if num % i == 0:
            cont += 1
        if cont > 2:
            return False
    return True

def lista_primos(num):
    lista = []
    for i in range(2, num+1):
        if is_prime(i):
            lista.append(i)
    return lista
    
    
    /*Agora a função is_prime é utilizada dentro da função lista_primos para verificar se cada número é primo ou não. 
    A função lista_primos percorre todos os números de 2 até num, e adiciona à lista apenas os números que forem 
    identificados como primos pela função is_prime.
    */
