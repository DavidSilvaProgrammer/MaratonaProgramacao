def ultimo_idx(lista, elemento):
    idx = None
    for i in range(len(lista)):
        if lista[i] == elemento:
            idx = i
    return idx

valor=3
lista = [0, 1, 2, 5, 3]

imprimir=ultimo_idx(lista,valor)

print(imprimir)
